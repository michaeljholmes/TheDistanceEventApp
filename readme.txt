Clone this Repo and install the packages(npm i)

To run on the webpack dev server, run 'npm run dev'.
To run on the node express erver, firstly run 'npm run build', then 'npm run start'.

Visit http://localhost:3001 to see the App

Select events for further information. Links to external webpages about the events are included at the top, if they exist.